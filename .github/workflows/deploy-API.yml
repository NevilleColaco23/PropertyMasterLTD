# ==============================================================================
# ⚠️ AUTOMATICALLY GENERATED BY AZURE DEPLOYMENT CENTER ⚠️
# ==============================================================================
# DO NOT DELETE THIS FILE!
#
# Purpose:
#   Builds and deploys the backend .NET Web API to Azure App Service:
#   App Name: propertymaster-api-de09
#
# Authentication:
#   Uses OpenID Connect (OIDC) via Azure User-Assigned Managed Identity.
#   It does NOT rely on traditional publish profiles or manual secrets.
#
# Note for Developers:
#   This file was generated by Azure App Service Deployment Center. Any breaking
#   changes to the Azure client IDs or permissions in this file will break
#   automated deployments for the API on pushes to 'Azure_web_App_Deployment'.
# ==============================================================================

# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - propertymaster-api-de09

on:
  push:
    branches:
      - Azure_web_App_Deployment
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read # This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Build with dotnet
        run: dotnet build ./WebApi/WebApi.csproj --configuration Release

      - name: dotnet publish
        run: dotnet publish ./WebApi/WebApi.csproj -c Release -f net6.0 -o "${{env.DOTNET_ROOT}}/myapp"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy:
    runs-on: windows-latest
    needs: build
    permissions:
      id-token: write # This is required for requesting the JWT
      contents: read  # This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_DC22BF29887A4A8381EAF4A8CC8FEB49 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_6E24F35AF622455FB1E9F2E1A8752EEC }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_D7C7FCC03F9840EE90600538A0C755D5 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'propertymaster-api-de09'
          slot-name: 'Production'
          package: .
